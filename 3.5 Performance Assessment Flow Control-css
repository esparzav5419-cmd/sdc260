/* =========================================
   NATIONAL PARK THEME PROVIDED
   DO NOT EDIT THIS FILE
========================================= */

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* =========================================
   COLOR PALETTE
   Inspired by National Park aesthetics:
   - deep forest greens
   - sandstone neutrals
   - sky accents
========================================= */

:root {
    --forest-dark: #1f3a2e;
    --forest: #2f5d3a;
    --forest-light: #3f7a4d;

    --sand: #f3efe3;
    --stone: #e6e0d4;

    --sky: #cfe8f3;
    --accent: #b08968;

    --text-dark: #1d2b1f;
}

/* =========================================
   BASE TYPOGRAPHY & BODY
========================================= */

body {
    font-family: Georgia, "Times New Roman", serif;
    background-color: var(--sand);
    color: var(--text-dark);
    line-height: 1.8;
}

body::before {
    content: "";
    position: fixed;
    inset: 0;
    background-image: radial-gradient(rgba(0,0,0,0.03) 1px, transparent 1px);
    background-size: 6px 6px;
    pointer-events: none;
    opacity: 0.4;
}

/* =========================================
   HEADER
========================================= */

header {
    background: linear-gradient(
        180deg,
        var(--forest-dark),
        var(--forest)
    );
    color: #fff;
    padding: 2.5rem 2rem;
    border-bottom: 6px solid var(--accent);
    text-align: center;
}

header h1 {
    font-size: 2.2rem;
    letter-spacing: 1px;
    margin-bottom: 0.5rem;
    text-transform: uppercase;
}

header p {
    font-style: italic;
    opacity: 0.9;
}

/* =========================================
   NAVIGATION
========================================= */

nav ul {
    list-style: none;
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 1rem;
    margin-top: 1.5rem;
}

nav a {
    text-decoration: none;
    color: white;
    padding: 0.4rem 0.9rem;
    border: 1px solid rgba(255,255,255,0.3);
    border-radius: 4px;
    transition: all 0.2s ease;
}

nav a:hover,
nav a:focus {
    background: rgba(255,255,255,0.15);
    border-color: white;
}

/* =========================================
   MAIN LAYOUT
========================================= */

main {
    max-width: 1100px;
    margin: auto;
    padding: 2rem 1.5rem;
}

section {
    margin-bottom: 3rem;
}

/* =========================================
   HEADINGS
========================================= */

h2 {
    font-size: 1.6rem;
    color: var(--forest-dark);
    border-left: 6px solid var(--accent);
    padding-left: 0.75rem;
    margin-bottom: 1rem;
}

/* =========================================
   HERO SECTION
========================================= */

section[aria-label="Introduction"] {
    background: linear-gradient(
        135deg,
        var(--sky),
        var(--stone)
    );
    padding: 3rem 2rem;
    border-radius: 10px;
    box-shadow: 0 6px 18px rgba(0,0,0,0.1);
}

section[aria-label="Introduction"] h2 {
    border: none;
    padding: 0;
}

/* =========================================
   GRID SYSTEM (TRAIL CARDS)
========================================= */

.grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1.2rem;
}

/* =========================================
   TRAIL CARDS
========================================= */

.card {
    background: linear-gradient(
        180deg,
        #ffffff,
        #f7f3ea
    );
    border: 1px solid #ddd6c8;
    border-left: 6px solid var(--forest-light);
    border-radius: 10px;
    padding: 1.2rem;
    box-shadow: 0 4px 10px rgba(0,0,0,0.08);
    transition: transform 0.2s ease;
}

.card:hover {
    transform: translateY(-4px);
}

.card h3 {
    color: var(--forest-dark);
    margin-bottom: 0.5rem;
}

/* =========================================
   FORM 
========================================= */

form {
    display: flex;
    flex-direction: column;
    max-width: 350px;
    gap: 0.6rem;
    margin-top: 1rem;
    background: white;
    padding: 1.5rem;
    border-radius: 10px;
    border: 1px solid #ddd6c8;
}

input,
select {
    padding: 0.5rem;
    border: 1px solid #ccc;
    border-radius: 6px;
    font-family: inherit;
}

/* =========================================
   BUTTON
========================================= */

button {
    margin-top: 1rem;
    padding: 0.7rem;
    background: var(--forest);
    color: white;
    border: none;
    border-radius: 6px;
    cursor: pointer;
    font-weight: bold;
    letter-spacing: 0.5px;
    transition: background 0.2s ease;
}

button:hover,
button:focus {
    background: var(--forest-dark);
}

/* =========================================
   RADIO BUTTON
========================================= */

fieldset label {
    margin-right: 1.5rem;
}

/* =========================================
   RESULTS PANEL
========================================= */

#result {
    margin-top: 1.5rem;
    padding: 1rem;
    background: var(--stone);
    border-left: 5px solid var(--accent);
    border-radius: 8px;
}

/* =========================================
   FOOTER
========================================= */

footer {
    margin-top: 3rem;
    background: var(--forest-dark);
    color: white;
    text-align: center;
    padding: 2rem;
    border-top: 5px solid var(--accent);
}

/* =========================================
   HIGH VISIBILITY FOCUS
========================================= */

:focus {
    outline: 3px solid #ffdd77;
    outline-offset: 3px;
}

/* =========================================
   RESPONSIVE DESIGN
========================================= */

@media (max-width: 768px) {

    header h1 {
        font-size: 1.6rem;
    }

    nav ul {
        flex-direction: column;
        align-items: center;
    }

    form {
        width: 100%;
    }
}
